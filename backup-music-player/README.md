# 音乐播放器备份

备份自 `front/components/` 的原始音乐播放器组件，在声场 (AmbientSound) 移植前使用。

## 文件说明

| 文件 | 说明 |
|------|------|
| `TopMusicPlayer.vue.bak` | 顶部音乐播放器组件（播放/暂停 + 进度条） |
| `Music.vue.bak` | 发布页面的音乐搜索/选择弹窗（MetingJS） |

## 还原方法

将文件复制回 `front/components/` 并去掉 `.bak` 后缀即可。
